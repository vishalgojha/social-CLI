{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "node bin/social.js --no-banner gateway --host 0.0.0.0 --port $PORT --require-api-key"
  }
}
